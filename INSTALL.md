# Install

Copy the contents of this folder into the repository:

GP-z007/GP-z007

Required:
- README.md
- assets/

That is all. There is no React app, local server, Vercel deployment, GitHub Action, or custom font installation.

The display typography is already rasterized into the PNG assets, so the GitHub README renderer does not need to load any custom fonts.
